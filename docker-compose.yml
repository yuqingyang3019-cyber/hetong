services:
  app:
    build:
      context: .
      dockerfile: agent/Dockerfile
    ports:
      - "9000:9000"
    env_file:
      - .env.local
    environment:
      PORT: "9000"
      H5_STATIC_DIR: /code/static
      PYTHONPATH: /code
    volumes:
      - ./agent/storage:/code/storage
